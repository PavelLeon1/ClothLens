segmentation:
  backend: unet
  checkpoint_path: models/unet_best.ckpt
  image_size: 512
  encoder: resnet34
  encoder_weights: null
  num_classes: 8
  in_channels: 3

embedding:
  model_name: patrickjohncyh/fashion-clip
  batch_size: 32

search:
  collection_name: clothing_catalog
  path: data/qdrant
  vector_size: 512
  top_k: 10
